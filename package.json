{
  "scripts": {
    "serve": "webpack-dev-server",
    "build": "cargo build --bin wasm --target wasm32-unknown-unknown && wasm-bindgen target/wasm32-unknown-unknown/debug/rusty_chip.wasm --out-dir web && webpack"
  },
  "devDependencies": {
    "webpack": "^4.0.1",
    "webpack-cli": "^2.0.10",
    "webpack-dev-server": "^3.1.0"
  }
}
